apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudmind-backend.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "cloudmind-backend.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "cloudmind-backend.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http

